from resources.graphics_engine.background_handler import draw_background as draw_background
def draw_medals_screen(game_display, info_getter, settings):
    draw_background(game_display, "almanac", settings)